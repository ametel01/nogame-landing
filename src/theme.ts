import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#28a745", // Customize primary color
    },
    secondary: {
      main: "#17a2b8", // Customize secondary color
    },
  },
});

export default theme;
